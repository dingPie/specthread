# specthread

**목차**

- [1. 개요](#1-개요)
- [2. SDD 작업 흐름](#2-sdd-작업-흐름)
- [3. 문서 체계](#3-문서-체계)
- [4. 미확정 마커](#4-미확정-마커)
- [5. 문서 작성 컨벤션](#5-문서-작성-컨벤션)

## 1. 개요

Spec-Driven Development (SDD) 템플릿. 문서가 단일 진실 원천이고 코드는 문서를 구현한다. 설정(`config.jsonc`)과 도구(`check`/`gen`)로 문서 체계를 지원한다.

- **spec 이 정본이다.** 코드와 충돌하면 spec 우선. 코드 변경이 시스템 동작을 바꾸면 spec 동시 갱신 의무.
- **설정**: `config.jsonc` (프로젝트 공유) + `config.local.jsonc` (개인, gitignore 대상).
- **도구**: `check` 는 규칙 위반을 검사하고, `gen` 은 참조 절·미확정 사항 뷰를 생성한다.

## 2. SDD 작업 흐름

```
1. 스펙 확인 → 2. 현황 점검(check) → 3. 계획 수립 → 4. 구현 → 5. 검증(check) → 6. 문서 갱신
```

- 구현 전에 spec 을 읽고, 구현 후에 spec 을 갱신한다.
- 진행 중 모호함을 발견하면 `PENDING` 마커를 남기고 작업을 계속한다 (§4).

## 3. 문서 체계

아래는 기본 경로다. `config.jsonc` 에서 override 했으면 그쪽이 우선한다.

| 기본 경로                | 역할                               |
| ------------------------ | ---------------------------------- |
| `specthread/rules.md`   | 문서 형식 규칙 (코어)              |
| `specthread/project.md` | 설정값의 맥락과 프로젝트 고유 설명 |
| `specthread/pending.md` | 미확정 사항 정본                   |

문서 유형 정의 (어떤 경로가 어떤 템플릿을 쓰는지) 는 `config.docTypes` 가 정본이다.

`docs/` 안 문서를 편집할 때는 `rules.md` 를 먼저 읽는다.

## 4. 미확정 마커

미완·임시 코드·데이터·디자인은 `PENDING::<종류>::<slug>` 마커로 명시한다. slug 가 `specthread/pending.md` 항목 하나를 가리킨다.

위치별 표기:

```
코드 / CSS        // PENDING::design::slug
JSON 데이터       "_pending": "design::slug"
Markdown 문서     본문에 `PENDING::design::slug` 인용
```

- 형식·검사 규칙: `rules.md` §4
- 종류 값: `config.markers.types` (`system` 은 코어 예약)
- 시점 라벨: `config.markers.timepoints`
- 종류별 맥락·처리자: `project.md` §2

## 5. 문서 작성 컨벤션

`docs/` 안 문서에 적용되는 핵심 규칙. 전체 규칙은 `rules.md`.

- **목차**: `## 1.` 이상 모든 번호 헤딩을 나열한다. 꼬리 절 (참조, 미확정 사항, 변경 이력) 은 무번호.
- **변경 이력**: 문서 하단에 `| 날짜 | 내용 |` 표. 한 커밋이 한 줄.
- **참조 절**: 도구가 본문 링크를 모아 생성한다. 사람이 쓰지 않는다.
- **빈 섹션**: `해당 없음 - <이유>`. 제목만 두지 않는다.
- **시점·버전 정보**: 본문이 아니라 변경 이력에 남긴다.
